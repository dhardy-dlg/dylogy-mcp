[project]
name = "dylogy-mcp"
version = "0.1.0"
description = "MCP server that auto-generates tools from the Dylogy OpenAPI spec"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
]